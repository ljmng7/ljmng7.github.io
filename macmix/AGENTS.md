# Prototype Instructions

The September 2026 MacMix home redesign starts with a full-screen animated pink-purple gradient from `#10002B` to `#E0AAFF`, inspired by the supplied app screenshot's background. Match `homepage/`'s implementation exactly: 36s drift, 24s bloom, identical stationary grain (256px tile, 19% opacity, soft-light), and scrolling behavior. The artwork slides upward until its bottom 68px plus safe-area inset remains pinned behind the top navigation. Reuse the homepage's mobile scroll-timeline implementation and support condition without additional browser-specific branches. Center a downward arrow above the text "下滑探索更多" near the bottom, reserving the header height plus the bottom safe-area inset underneath. Loop their simultaneous fade-in, a one-second stationary hold, the text fading out, then the arrow moving down nonlinearly and fading out before the next cycle. Reduced-motion mode keeps this cue static. The home navigation restores the original brand, version badge, Changelog, and compact Download controls over the existing gradient. Keep its 68px height plus safe-area inset, use 44px-high right-side controls with 15px desktop labels, and keep their foreground white for contrast. Preserve the original mobile simplification (icon-only Changelog, hidden Download) and the existing navigation on legal routes. Changelog uses this same 68px gradient header and controls, showing the top slice of the full-height gradient immediately with no full-screen intro or scroll-to-collapse. Its header stays pinned while the release content scrolls. Compact Download uses a 180px minimum width, 16px side padding, an 8px icon-to-label gap, and a matching 28px hover label shift. This supersedes the earlier temporary empty-header requirement. Match the homepage page background (`#F9F9F7` light, `#1C1C1A` dark). Preserve the existing content below the new gradient intro.

Keep gradient-header scrolling CSS-driven, matching the personal homepage: do not run the legacy glass scroll-progress listener on home or Changelog, and leave the idle page-content container without filter, transform, or will-change promotion. Preserve the existing route leaving/entering transitions and all button animations.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

The gradient scroll hint matches the personal homepage in both Chinese ("下滑探索更多") and English ("Scroll to explore more"). Use the saved language preference when available, otherwise the browser language; the personal homepage uses its existing live language switch. Both hints use a 4s cycle: 600ms simultaneous fade-in, exactly 1s stationary hold, then text fading out over 400ms while the arrow simultaneously moves down 28px over 800ms with nonlinear easing. The arrow then fades over 800ms, followed by an 800ms fully invisible gap before repeating. This simultaneous exit supersedes the earlier sequential text/arrow requirement.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

This site now lives in `macmix/` in the Jazmín websites repository. Keep its
source, assets, lockfile, and build independent of `homepage/` and `yumchicken/`.
The root workflow currently publishes only the homepage; MacMix deployment and
subdomain configuration are intentionally deferred. Preserve the environment-driven
`SITE_BASE`, base-aware assets and internal routes, and `dist/client/404.html`.
The static output remains `macmix/dist/client`; the Sites-compatible server and
hosting output remain available. Do not commit `node_modules`, `dist`, or the two
unused 6K wallpaper PNG sources. Keep web assets below 100 MiB per file.

For the animated download callout, do not use `vector-effect: non-scaling-stroke` together with dash-based path drawing; Chromium can truncate the settled stroke even when Safari renders it fully.

Preserve the user-adjusted desktop download-callout drawing sequence: the primary stroke begins at `0.35s`, its echo at `0.42s`, and the arrowhead at `0.64s`. The `Free!` label begins its nonlinear blur/fade at `1.18s`, immediately after the main stroke finishes; do not push it back to the former `1.36s` delay. Keep the user-selected title casing, `32px` label size, and SVG baseline at `y=49`. Keep the entire callout SVG below the Download button's stacking layer so any overlapping arrow stroke is covered by the button. Hide the entire callout, including its arrow and `Free!` label, on mobile.

The mobile homepage hero (`≤640px`) uses the dedicated two-line title `Mac's sound.` / `In one Mix`; its first line reuses the desktop Apple-mark lockup sizing, baseline alignment, and Mac highlight, while omitting the desktop-only `Your`. Keep `Mix` rendered through the same `DiaTextReveal` effect used on desktop. Its responsive title size is `clamp(36px, 10.8vw, 46px)`. Mobile hero top padding is `48px` below the `76px` header, and its action group starts `44px` below the title. Preserve the user-adjusted `54px` GitHub-button-to-preview gap. The mobile hero has no bottom padding; the features section owns the following `54px` block gap through its top padding. Keep both stacked mobile hero CTAs and the mobile footer download CTA centered at `min(100%, 260px)` so their internal side space stays close to the desktop buttons. Desktop hero copy, Mac highlight, callout, spacing, and typography remain unchanged. Keep the homepage document title and meta description aligned with the exact `Mac's sound. In one mix.` positioning.

The MacMix gradient intro opens with the large title alone at the exact viewport center. Reveal it over 1300ms, then move it nonlinearly upward over 1200ms with `cubic-bezier(.45, 0, .2, 1)` into the established final layout. Fade in the action buttons over 1100ms, hold the completed title-and-buttons composition for 400ms, then start the arrow and localized scroll-hint cycle. On mobile, calculate the title's starting center from the full stacked action-group height.

The desktop header should remain transparent at the top of the page, then progressively reveal its warm glass backdrop and brown lower shadow while scrolling. Preserve the user-adjusted 78%-opaque header tint and 10%-opaque `--button-glass` tint. Complete the scroll reveal by about `84px`. Header navigation pills use the glass material on hover; the compact download pill keeps that same background on hover instead of darkening. `View on GitHub` shares the same resting tint, while retaining its own blur because it is outside the header. Avoid nested backdrop filters inside the already-blurred header, and do not add a hard bottom divider.

Keep the entire hero `View on GitHub` button completely static across rest, hover, and keyboard focus. Its glass shell, GitHub mark, and full label remain in their resting appearance with no background, color, opacity, scale, shadow, gradient, or position animation. Inside the button, use the same centered `184px` content track as the download button: pin the GitHub mark to the track's left edge so it aligns with the Apple logo, and center the label within the remaining right-hand area. Apply this internal alignment on desktop and mobile. Do not restore the expanding dark fill, alternate `GitHub` label, arrow, content translation, Aurora treatment, Animated Gradient Text, or layered opacity crossfade.

On initial load, reveal the header brand, Changelog link, and compact download button as three independently staggered Blur Fade items. Keep the motion subtle (`6px` offset) and preserve their settled layout. The primary hero download CTA uses identical resting and hover shadows with brown layers at `rgba(111, 75, 48, 0.42)` and `rgba(111, 75, 48, 0.24)`; hovering must not deepen it further.

The header brand mark uses the packaged macOS app icon at `/assets/MacMix/MacMix-macOS-Default-web-256.png`, with no extra frame, outline, inset stroke, or shadow. It is `40px` square on desktop and `36px` square on mobile. Keep the icon fixed across hover and keyboard focus; do not swap it for an arrow or animate its content. The desktop version pill is `36 × 20px`, top-aligned with the visible icon, and flex-centers its `12px`, `700`-weight brown label. Its `1.5px` resting border is `20%`-opaque brown and direct hover raises only that border to `50%`; the `--button-glass` background stays unchanged. Keep the version pill visible on mobile at approximately `30 × 17px`, with the same top alignment and centered bold label. The mobile header glass reveal completes by about `64px` of scroll; desktop remains about `84px`.

Use that same Default app icon for the site favicon and Apple Touch Icon on every route, including `/changelog`; do not switch the browser icon for dark mode.

The Changelog is an internal SPA route at `/changelog`. On desktop, its 23px icon tilts left by `15deg` on hover without moving the button. Clicking morphs only the control's contents: the newspaper and “Changelog” blur left, while the `/changelog` route retains the same outer control as “Back home”. Its default arrow sits on the right; hover blurs that arrow out to the right, shifts the label right, and reveals an identical left arrow. Both desktop icon slots use a `23px` inset to align with the compact download button, and the arrows do not scale. Portrait iPad widths (`768px–980px`) explicitly keep this complete desktop Changelog control beside the compact Download button instead of inheriting the narrower breakpoint's hidden state. On mobile (`≤640px`), hide the compact Download control and give the Changelog route control its `48 × 48px`, `15px`-radius glass shell. Mobile never displays either route label or the duplicate left arrow: home shows only the centered `text.square.and.bookmark.fill.svg`, which blurs and exits left when activated, while the single centered Back home arrow blurs in from the right. Reverse navigation plays the complementary direction. Disable both the Changelog icon's desktop hover tilt and the desktop Back home arrow-swap behavior on mobile; the single arrow must remain centered and visible while hovered or keyboard-focused. The page body still fades before the route commit. Release entries and the brand version come from `src/generated/github-releases.json`, generated from the GitHub Releases API by Actions before Vite compiles it into the deployed bundle. The browser must not fetch the GitHub API, fetch a separate Release JSON, or use `localStorage` for Release data at runtime. The brand badge displays only major and minor (`v1.3.1` becomes `v1.3`), while timeline entries retain the full release tag.

The Changelog page intro contains only the `Changelog` heading—do not restore the “Release notes” eyebrow or descriptive sentence. Its heading must use exactly the same responsive font size, weight, line height, and letter spacing as the homepage hero heading. The release timeline uses one visually continuous vertical rail with a 1px overlap between entries; keep the approved entry padding while omitting horizontal separators.

The `/privacy-policy` and `/terms-of-use` pages use the Changelog page as their visual source of truth: preserve its page width, title typography, responsive spacing, color treatment, body typography, and the same fixed 68px purple gradient header (including its controls and page background). Their titles are static text and must not use the Changelog `HyperText` scramble effect. Only their legal body content should differ from the Changelog presentation.

The header brand icon and “MacMix” name are a same-page `#top` link on both routes; they must never navigate away from `/changelog`. The version pill is a separate `/changelog` link: from home it uses the existing page transition, and on `/changelog` it scrolls to the top. Changelog and Back home route controls must always commit their destination at `scrollY = 0`. Do not restore global `html { scroll-behavior: smooth; }`: same-page controls request smooth scrolling explicitly, while route changes use one immediate `window.scrollTo(0, 0)` so Safari and Chromium share the same deterministic behavior.

Render the Changelog `h1` as fixed plain text. Do not use the Magic UI-style `HyperText` scramble effect on mount or hover. Preserve the title's approved homepage-matched typography without a BlurFade wrapper.

Do not reintroduce `BlurFade` or the simulated desktop preview (including its wallpaper, Dock, Music/Safari windows, and MacMix panel) unless the user explicitly requests it.

The homepage headline and its Download / Upgrade Studio controls form one vertically centered group inside the full-screen gradient intro, not in the content below it. Use a desktop row and mobile stack without duplicating the controls after the gradient. Omit the former animated `Free!` callout entirely. In this intro only, the Download button uses a solid white background with dark content and no shadow. Upgrade Studio links to the exact live checkout URL ending in `pdt_0NmJ2vOqulR07tfZqwKtU?quantity=1`; only `Studio` uses a serif face, enlarged to `1.16em` with a compact `0.9` line height so it reads taller without changing button height. Its packaged `checkmark.seal.fill.svg` icon rests at 70%-opaque white, then becomes 100%-opaque white on hover or keyboard focus. Animate that alpha change through one `background-color` transition rather than a separate element-opacity transition, so Safari cannot desynchronize compositing. Preserve the current translucent white button treatment. In light mode, the footer Download button uses the gradient's deep purple `#10002B` with a two-layer shadow derived from its pink `#E0AAFF`; in dark mode, swap those colors so the button is pink with deep-purple content and shadow. Retain the existing shadow geometry and hover parity. Render the desktop lines as plain, white `Your Mac's sound.` / `All in MacMix.` copy with the final period; at `≤640px`, omit only the `Your` prefix and keep `Mac's sound.` / `All in MacMix.`. Do not wrap any part of this headline in Magic UI-style Highlighter, DiaTextReveal, BlurFade, or similar effects. Fade the complete settled headline in over 1600ms with the same easing and 200ms delay as the personal homepage's Hello copy; do not add translation, blur, drawing, highlighting, or per-word animation.

For non-compact Chinese Download controls on the homepage and footer, do not center the complete logo-and-label group. Pin the Apple logo to the shared 23px internal rail, then end the Chinese Download content track 12px before the Upgrade Studio track's far edge so its text and hover arrow sit less far right. On hover, move the label's left edge to the original Apple-logo position while leaving the arrow's right edge aligned to that original label endpoint. Do not alter the compact header Download control.

Keep the animated gradient on dedicated compositor layers in Safari: the drift and bloom transforms use `translate3d`, with persistent `will-change` and hidden backfaces. The Upgrade Studio label must not use percentage vertical translation; center its fixed inset box with flex layout and keep that text layer promoted from rest through hover. Isolate the icon's mask/color transition on its own paint/compositor layer so hovering it never rerasterizes or shifts the label.


The homepage features are an Apple-inspired horizontal carousel, replacing the old eight-icon grid and its dedicated React icon components. Use four placeholders initially, with each feature title above a 4:3 media area and reserved title spacing. Keep neighboring slides partially visible and the dot/progress capsule plus separate pause/play/replay button centered below the media in normal document flow. Start once the carousel scrolls into view. Each slide has its own five-second carousel clock, independent of video playback: pausing the carousel does not pause its current video; ended video stays on its final frame, including after resuming the carousel. End after the last slide and offer replay. Use muted inline videos without native controls, looping, or Media Session registration; disable remote playback and picture-in-picture. Preserve the rest of the page.

The homepage ends with a centered reuse of the primary Download for Mac control, followed by the direct social/contact links from the personal homepage: GitHub, 小红书, 抖音, X, Instagram, and Email. Keep all three footer gaps compact without changing the final `MacMix` wordmark's anchor: desktop uses `36px` footer top padding and an `80px` Download-to-social grid gap, while mobile uses `32px` top padding and an `84px` internal gap. Size the footer itself from the wordmark's responsive height—`clamp(340px, calc(12.21vw + 248px), 446px)` on desktop and `clamp(305px, calc(16.5vw + 244px), 331px)` on mobile—so the social row remains about `30px` above the fixed wordmark. At the standard viewports this yields roughly `84px` desktop / `76px` mobile from the last feature to Download, `80px` desktop / `84px` mobile from Download to the social row, and `30px` from the social row to the wordmark. Use the supplied black Figma social SVG assets inside restrained glass squares: `42px` buttons with `20px` art on desktop and `38px` buttons with `18px` art on mobile. Social button shells remain completely stationary on hover/focus—only the inner icon darkens to `82%` opacity and scales to `1.06`. The final `MacMix` wordmark remains anchored at its original `-0.1em` bottom offset, remains a single `--ink` color, uses an `850` weight and the user-approved looser `-0.03em` letter spacing, and is intentionally quieter than the first footer pass at `8.5%` base opacity. Desktop keeps `clamp(140px, 18.5vw, 300px)` and mobile keeps `clamp(92px, 25vw, 132px)`; only the `641px–1120px` range expands toward the smaller of the desktop maximum and the viewport-safe width via `min(300px, calc((100vw - 16px) * 0.2745))`. Compensate the footer height across that range by the exact added line-box height so enlarging the wordmark never changes its visual gap from the social row. Its additional fade is created only by an opacity mask, never a color gradient: the mask is down to `42%` strength near `39%` of the wordmark height and reaches fully transparent at `90%`, keeping the bottom quieter without erasing it too early. Preserve the small transparent inline padding around the wordmark so the final `x` is never clipped by the footer overflow boundary.

Keep the homepage header transparent over the scrolling full-screen gradient, matching `homepage/`: it must not contain a viewport-height gradient backdrop, clip-path, or header-wide overlay. Changelog has a fixed 68px header whose own `::before` paints the static top slice of the same gradient; this is the only route-specific background. The page transition may fade content, but it must not hold or leave a gradient overlay over the homepage. Keep the homepage gradient scrolling dimensions and scroll-timeline rules aligned with homepage/.

Carousel refinement: slide transitions take 1100ms. Use the supplied play.fill.svg, pause.fill.svg, and arrow.clockwise.svg assets for controls. Match the Apple iPhone highlights gallery with native horizontal overflow scrolling and centered mandatory CSS scroll snap. Touch and trackpad gestures use browser momentum and snapping; do not intercept them with custom thresholds, wheel-to-slide handlers, or transform dragging. Use the normal cursor and no custom mouse-drag mode, matching the inspected Apple gallery. Dot/keyboard/autoplay navigation animates scrollLeft over 1100ms; temporarily disable snap during that animation, restore it afterward, and allow native input to interrupt. Manual navigation or pagination clicks pause autoplay and show Play. Play resumes the current slide clock without restarting the sequence or video. Show Replay only after the final slide completes its five-second progress. Play and Pause glyphs are 20px; Replay stays 25px with a thicker stroke. Mobile media uses a 3:4 frame with centered cover cropping of the original 4:3 media; desktop stays 4:3.

Footer contact links match homepage Contact in this order: GitHub, X, Threads, 小红书, Instagram, Email. Use the same URLs, including `https://x.com/jazminli57`, `https://www.threads.com/@lucid.jasmine`, and `mailto:jasmine@jazminli.com`. Package the supplied Threads SVG locally. Keep the existing img elements and opacity/scale hover animation, using interpolated CSS filters only to recolor the artwork; hover and keyboard focus use the gradient endpoint `#10002B` in light mode and `#E0AAFF` in dark mode at full opacity, retaining the stationary glass shells and subtle icon scale. This supersedes the earlier footer link order and 82%-opacity hover.
