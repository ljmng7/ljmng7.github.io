# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

This site now lives in `macmix/` in the Jazmín websites repository. Keep its
source, assets, lockfile, and build independent of `homepage/` and `yumchicken/`.
The root workflow currently publishes only the homepage; MacMix deployment and
subdomain configuration are intentionally deferred. Preserve the environment-driven
`SITE_BASE`, base-aware assets and internal routes, and `dist/client/404.html`.
The static output remains `macmix/dist/client`; the Sites-compatible server and
hosting output remain available. Do not commit `node_modules`, `dist`, or the two
unused 6K wallpaper PNG sources. Keep web assets below 100 MiB per file.

For the animated download callout, do not use `vector-effect: non-scaling-stroke` together with dash-based path drawing; Chromium can truncate the settled stroke even when Safari renders it fully.

Preserve the user-adjusted desktop download-callout drawing sequence: the primary stroke begins at `0.35s`, its echo at `0.42s`, and the arrowhead at `0.64s`. The `Free!` label begins its nonlinear blur/fade at `1.18s`, immediately after the main stroke finishes; do not push it back to the former `1.36s` delay. Keep the user-selected title casing, `32px` label size, and SVG baseline at `y=49`. Keep the entire callout SVG below the Download button's stacking layer so any overlapping arrow stroke is covered by the button. Hide the entire callout, including its arrow and `Free!` label, on mobile.

The mobile homepage hero (`≤640px`) uses the dedicated two-line title `Mac's sound.` / `In one Mix`; its first line reuses the desktop Apple-mark lockup sizing, baseline alignment, and Mac highlight, while omitting the desktop-only `Your`. Keep `Mix` rendered through the same `DiaTextReveal` effect used on desktop. Its responsive title size is `clamp(36px, 10.8vw, 46px)`. Mobile hero top padding is `48px` below the `76px` header, and its action group starts `44px` below the title. Preserve the user-adjusted `54px` GitHub-button-to-preview gap. The mobile hero has no bottom padding; the features section owns the following `54px` block gap through its top padding. Keep both stacked mobile hero CTAs and the mobile footer download CTA centered at `min(100%, 260px)` so their internal side space stays close to the desktop buttons. Desktop hero copy, Mac highlight, callout, spacing, and typography remain unchanged. Keep the homepage document title and meta description aligned with the exact `Mac's sound. In one mix.` positioning.

The desktop header should remain transparent at the top of the page, then progressively reveal its warm glass backdrop and brown lower shadow while scrolling. Preserve the user-adjusted 78%-opaque header tint and 10%-opaque `--button-glass` tint. Complete the scroll reveal by about `84px`. Header navigation pills use the glass material on hover; the compact download pill keeps that same background on hover instead of darkening. `View on GitHub` shares the same resting tint, while retaining its own blur because it is outside the header. Avoid nested backdrop filters inside the already-blurred header, and do not add a hard bottom divider.

Keep the entire hero `View on GitHub` button completely static across rest, hover, and keyboard focus. Its glass shell, GitHub mark, and full label remain in their resting appearance with no background, color, opacity, scale, shadow, gradient, or position animation. Inside the button, use the same centered `184px` content track as the download button: pin the GitHub mark to the track's left edge so it aligns with the Apple logo, and center the label within the remaining right-hand area. Apply this internal alignment on desktop and mobile. Do not restore the expanding dark fill, alternate `GitHub` label, arrow, content translation, Aurora treatment, Animated Gradient Text, or layered opacity crossfade.

On initial load, reveal the header brand, Changelog link, and compact download button as three independently staggered Blur Fade items. Keep the motion subtle (`6px` offset) and preserve their settled layout. The primary hero download CTA uses identical resting and hover shadows with brown layers at `rgba(111, 75, 48, 0.42)` and `rgba(111, 75, 48, 0.24)`; hovering must not deepen it further.

The header brand mark uses `/assets/MacMix/MacMix.png` inside one custom black rounded-square frame rather than the packaged macOS app icon. The four corners share the same `11px` radius, the visible desktop frame is about `40px` square, its user-adjusted border is `2.5px`, and it has no shadow. Do not add a second outline or inset stroke. The desktop version pill is `36 × 20px`, top-aligned with the visible icon frame, and flex-centers its `12px`, `700`-weight brown label. Its `1.5px` resting border is `20%`-opaque brown and direct hover raises only that border to `50%`; the `--button-glass` background stays unchanged. Keep the version pill visible on mobile at approximately `30 × 17px`, with the same top alignment and centered bold label. The mobile header glass reveal completes by about `64px` of scroll; desktop remains about `84px`.

Hovering or keyboard-focusing the brand icon/name link swaps only the content inside the icon frame: the MacMix image moves `18px` upward while blurring and fading out, and a `24px` upward arrow enters from `18px` below through the inverse blur/fade. Keep the frame and “MacMix” name stationary. Match the download button's staggered `280–440ms` transitions and shared nonlinear `--ease`; the mobile arrow is `22px`.

The Changelog is an internal SPA route at `/changelog`. On desktop, its 23px icon tilts left by `15deg` on hover without moving the button. Clicking morphs only the control's contents: the newspaper and “Changelog” blur left, while the `/changelog` route retains the same outer control as “Back home”. Its default arrow sits on the right; hover blurs that arrow out to the right, shifts the label right, and reveals an identical left arrow. Both desktop icon slots use a `23px` inset to align with the compact download button, and the arrows do not scale. Portrait iPad widths (`768px–980px`) explicitly keep this complete desktop Changelog control beside the compact Download button instead of inheriting the narrower breakpoint's hidden state. On mobile (`≤640px`), hide the compact Download control and give the Changelog route control its `48 × 48px`, `15px`-radius glass shell. Mobile never displays either route label or the duplicate left arrow: home shows only the centered `text.square.and.bookmark.fill.svg`, which blurs and exits left when activated, while the single centered Back home arrow blurs in from the right. Reverse navigation plays the complementary direction. Disable both the Changelog icon's desktop hover tilt and the desktop Back home arrow-swap behavior on mobile; the single arrow must remain centered and visible while hovered or keyboard-focused. The page body still fades before the route commit. Release entries and the brand version come from `src/generated/github-releases.json`, generated from the GitHub Releases API by Actions before Vite compiles it into the deployed bundle. The browser must not fetch the GitHub API, fetch a separate Release JSON, or use `localStorage` for Release data at runtime. The brand badge displays only major and minor (`v1.3.1` becomes `v1.3`), while timeline entries retain the full release tag.

The Changelog page intro contains only the `Changelog` heading—do not restore the “Release notes” eyebrow or descriptive sentence. Its heading must use exactly the same responsive font size, weight, line height, and letter spacing as the homepage hero heading. The release timeline uses one visually continuous vertical rail with a 1px overlap between entries; keep the approved entry padding while omitting horizontal separators.

The `/privacy-policy` and `/terms-of-use` pages use the Changelog page as their visual source of truth: preserve its page width, title typography, responsive spacing, color treatment, shared header, and body typography. Their titles are static text and must not use the Changelog `HyperText` scramble effect. Only their legal body content should differ from the Changelog presentation.

Keep `blur={false}` on the Changelog intro's `BlurFade`. The heading should retain opacity and vertical movement, but its inner wrapper must not animate a CSS blur: Safari/WebKit temporarily clipped the bottom of the final `g` when that blur was nested inside the page-level blur transition. The user confirmed the no-inner-blur version resolves the Safari artifact.

The header brand icon and “MacMix” name are a same-page `#top` link on both routes; they must never navigate away from `/changelog`. The version pill is a separate `/changelog` link: from home it uses the existing page transition, and on `/changelog` it scrolls to the top. Changelog and Back home route controls must always commit their destination at `scrollY = 0`. Do not restore global `html { scroll-behavior: smooth; }`: same-page controls request smooth scrolling explicitly, while route changes use one immediate `window.scrollTo(0, 0)` so Safari and Chromium share the same deterministic behavior.

Render the Changelog `h1` through the local Magic UI-style `HyperText` component. It scrambles on first mount and replays on pointer hover, then restores the exact `Changelog` string while keeping `aria-label="Changelog"`. Preserve the title's approved homepage-matched typography and continue using `blur={false}` on its surrounding BlurFade.

HyperText's scramble casing follows the final title: the first non-space character must always be uppercase and every later scrambled character lowercase. Do not restore the all-uppercase default character display; the settled value remains exactly `Changelog`.

The first desktop-preview stage uses the full Golden Gate wallpaper, not the previous MacMix screenshot. Serve `/assets/MacMix/GoldenGate-1600.webp` and `GoldenGate-2560.webp` as high-quality responsive derivatives of the `6016 × 3760` 16:10 PNG. The desktop mask is the existing `1110px`-wide preview slot at `16:9` and uses the shared `SmoothCorner` SVG clip with a `58px`, exponent-`3.2` continuous curve. Do not restore experimental CSS `corner-shape`; Safari and Chrome must consume the same generated path. Its scroll crop has three invariants: page top = wallpaper top aligned, mask centered in the viewport below the sticky header = wallpaper centered, and mask center reaching the header bottom = wallpaper bottom aligned. Preserve the half-viewport desktop scroll runway below this first-stage mask so the final alignment remains reachable until a later content section replaces it. The mask's bottom-centered Dock follows the Magic UI example structure and spring behavior with `50px` resting DockIcon containers, `75px` magnification, `175px` cursor distance, and fixed `42px` MacMix, Music, and Safari PNG art inside each container. Its `72px` glass frame uses the shared dynamic `SmoothCorner` clip at `27px`, a `20px` blur, a `20%` white fill, and a path-following `16%` white outline; keep it `10px` from the preview bottom. The path must resize during the Dock spring animation while the artwork remains fixed. On desktop, adapt the Magic UI registry `Safari` frame in `simple` mode to an integer `1200 × 952` shell with its original `52px` toolbar and a true `1200 × 900` 4:3 content aperture; keep it at `56%` of the preview width above the Dock. Compose the local media underneath the toolbar: the Rick Astley MP4 at the existing `_1080p.mp4` path is a web-optimized `1280 × 960` H.264/AAC file, derived from the centered 4:3 crop and encoded without B-frames for deterministic Safari pause behavior. Keep the 4:3 source centered and allow only a one-pixel masked overscan around the native video surface to hide Safari compositor seams; do not otherwise crop or stretch it. In media mode, omit the SVG mask and Safari body fill entirely: the toolbar and DOM video share the wrapper's single rounded clipping surface so Safari cannot create a light antialias seam between separately composited layers. The Safari frame is only the visual shell; use the browser-native `<video controls>` UI for playback, pause, seeking, volume, fullscreen, keyboard behavior, and decoder state. Do not add custom media controls, playback state, or frozen-frame layers. It starts paused. Do not substitute other icon art. Preserve the original line/arrow assets and do not add other macOS chrome until requested. Mobile crop and pan behavior is specified separately below.

The desktop preview's MacMix popover must mirror `MacMix/MacMixPanel.swift`, not a screenshot-only approximation. Preserve the native `320pt` panel width, `14pt` inset, outer `12pt` vertical rhythm, `28pt` system/input slider rows, `26pt` device icon rows with `12pt` icon-to-label spacing, `13pt medium` labels, `34pt` trailing percentage column, and `24pt + 60pt + slider + 34pt` Mix row grid. Output's collapsible content owns both its device list and Mix; Input owns its device list and input slider. The website demo intentionally limits each device list to AirPods Pro, EarPods, and MacBook Pro and Mix to Music and Safari, while omitting the native bottom action buttons. Both input and output default to MacBook Pro. Preserve an independent saved volume for every input and output device: changing a selected device updates only its stored value, switching output devices immediately drives System Volume from the selected device, and switching input devices immediately drives the input slider from the selected device. Use the supplied `speaker.fill.svg` and `microphone.and.signal.meter.fill.svg` for the native slider endpoints, and the dedicated `Music-iOS-Default-32@2x.png` and `Safari-iOS-Default-32@2x.png` assets inside Mix. Mix uses the native conditional tint logic: blue through 100%, yellow above 100%, with a unity tick and hover-to-restore percentage control. System volume drives the menu-bar speaker asset thresholds from `OutputAudioState.menuBarSymbolName` and scales both demo apps' real media volume.

On mobile (`≤640px`), keep the simulated desktop as a tall `620px` frame containing the centered native-width MacMix panel. Hide the Music window, Safari window, and Dock, but retain the wallpaper and menu bar. Treat the mobile wallpaper as a centered crop of the same 16:10 image used by the desktop preview. Give it exactly one tenth of the frame height (`62px`) of vertical overscan and translate it only through that range with `--preview-scroll-progress`; do not freeze its scroll progress at zero. Compress the menu-bar status-item margins, and hide only the weekday while preserving the live `HH:mm` time. Desktop preview geometry and content remain unchanged.

For tablet and narrow-desktop widths (`641px–1120px`), preserve the preview's `16:9` mask instead of making it taller. Set `--preview-layout-scale` from the live preview width divided by the canonical `1110px` width, clamped to `0.5–1`, and use it to scale the native `320px` MacMix panel from its top-right corner. Reduce the active Music/Safari slot to `50%` so the app, scaled panel, and centered Dock have non-overlapping regions. Keep the Free callout visible by fluidly increasing its inset as the viewport narrows; do not hide it at tablet widths. Portrait tablet/narrow-desktop layouts in this range use the mobile two-line hero copy and effects, `66px` hero top padding, and a larger `clamp(72px, 10vw, 88px)` title; landscape layouts keep the desktop title. Mobile and large-desktop preview rules remain unchanged.

Normalize every speaker asset by the physical speaker base rather than by its full SVG view box. The base is exactly 13px high for mute and all wave counts, while waves extend only to the right; this applies to both the panel endpoints and the menu-bar MacMix volume icon. Center panel endpoint symbols inside their native 24px frames; keep the dynamic menu-bar speaker base left-anchored so added waves extend right without shifting that base. The menu-bar volume capsule uses a pure-white mask while preserving its theme-specific opacity: `40%` in light mode and `22%` in dark mode. Give only that capsule a `14px` background blur with `1.12` saturation, including the WebKit-prefixed filter for Safari; do not change its dimensions, corner radius, opacity, or icon treatment. Match native SF Symbol optical sizing inside every 26px device circle rather than forcing a shared square: AirPods Pro and the corrected EarPods asset are 13px tall, MacBook Pro is 11px tall, and input microphones are 14px tall. The reference screenshot's EarPods headphone glyph is known to be wrong; keep using the supplied `earpods.svg` while matching its native 13px optical slot. Native-style sliders use a 5px continuous pill track and a 19px circular thumb. Keep the range painting box 28px high with visible overflow so Mix thumb shadows are never clipped, while the 20px shell preserves the original track and unity-tick centerline. Give the Mix list a 4px internal paint gutter canceled by a -4px block margin so the native row positions and section spacing do not move.

Music and Safari now occupy one shared desktop app slot rather than appearing together. Both measure exactly `56%` of the preview at the same `4.5%` left inset and `104px` bottom inset, using Safari's `1200 / 952` outer ratio. Music is the default visible app. Its former title bar is removed; the interior remains vertically stacked as cover, metadata, progress, and previous/play/next controls. Both Music audio and Safari video must start paused and must never autoplay on page load; playback begins only from an explicit user action. A track change may resume only when the user was already playing Music. Clicking the Dock's Music or Safari icon switches the shared slot with overlapping opacity and `10px` blur transitions using `--ease`; the outgoing and incoming media trees remain mounted so playback state is preserved. The inactive app is non-interactive and becomes hidden after the transition. Keep the Dock at the user-adjusted `10px` bottom inset. The MacMix Dock icon remains reserved for the future mixer window.

The homepage features follow the interactive desktop as an open, heading-free `4 × 2` icon grid rather than cards. Keep the eight MacMix-specific benefits concise, center-aligned, and visually consistent with the supplied SF Symbols-style assets; collapse to `2 × 4` on narrower screens. Normalize these differently proportioned symbols by their actual visible height—not merely a shared square wrapper—at approximately `60px` on desktop and `48px` on mobile. Do not apply a shared hover enlargement to feature icons. Feature icon opacity uses three intentional levels: normal and hover-complete layers are 100%, explicitly weakened resting layers are 40%, and AirPods internal detail layers remain 42.5%; preserve animation-only 0% states used for crossfades. When a supplied feature SVG later needs animation, inline its original path geometry in a dedicated React component at that time; do not proactively convert untouched feature assets. The first “App-by-app volume mix” component preserves `slider.horizontal.3.svg`: its three tracks and overall geometry remain fixed while the top and bottom thumbs exchange horizontal positions with the middle thumb using a nonlinear transition and the asset's exact `344.75`-unit offset. The second component preserves `speaker.wave.3.svg`: “Boost up to” stays on one line, its outer two waves rest at 40% opacity, then rise from inner to outer to 100% on hover. The third “Instant device switching” component preserves the original `airpods.pro.svg` and `airpods.gen4.svg` paths: the left AirPods Pro remains fixed while only the right Pro moves down and fades into a size-matched right AirPods Gen 4 moving upward in the same fixed slot. The fourth “Independent input gain” component preserves `microphone.and.signal.meter.svg`: its top three meter dots rest at 40%, rise from bottom to top to 100% on hover, and return from top to bottom on pointer exit while the icon stays fixed. The fifth “One-click muting” component preserves `speaker.slash.svg`: the speaker body stays fixed, the slash rests at 40%, and a nonlinear clipped overlay restores it progressively to 100% from the slash's upper-left endpoint toward its lower-right endpoint. The sixth “Always in the menu bar” component reuses the preserved `switch.2.svg` component formerly shown first: its tracks, colors, position, and size remain fixed while only the two thumbs swap sides with nonlinear motion. The seventh “Crazy fast native app” component preserves `bolt.bottomhalf.filled.striped.horizontal.tophalf.svg`: the bottom bolt remains at 100% while the four upper stripes rest at 40%, rise from bottom to top to 100% on hover using the same `180ms` nonlinear transition and `60ms` stagger as the microphone meter, and return from top to bottom on pointer exit. The eighth “Free and open source” feature keeps its normalized GitHub mark permanently in `--ink`; it has no hover color, opacity, scale, or movement effect. This downstream section replaces the former half-viewport empty scroll runway, so the hero now keeps only a compact `112px` desktop gap after the interaction hint while the feature section's own height preserves the wallpaper's reachable bottom-aligned scroll state.

The homepage ends with a centered reuse of the primary Download for Mac control, followed by the direct social/contact links from the personal homepage: GitHub, 小红书, 抖音, X, Instagram, and Email. Keep all three footer gaps compact without changing the final `MacMix` wordmark's anchor: desktop uses `36px` footer top padding and an `80px` Download-to-social grid gap, while mobile uses `32px` top padding and an `84px` internal gap. Size the footer itself from the wordmark's responsive height—`clamp(340px, calc(12.21vw + 248px), 446px)` on desktop and `clamp(305px, calc(16.5vw + 244px), 331px)` on mobile—so the social row remains about `30px` above the fixed wordmark. At the standard viewports this yields roughly `84px` desktop / `76px` mobile from the last feature to Download, `80px` desktop / `84px` mobile from Download to the social row, and `30px` from the social row to the wordmark. Use the supplied black Figma social SVG assets inside restrained glass squares: `42px` buttons with `20px` art on desktop and `38px` buttons with `18px` art on mobile. Social button shells remain completely stationary on hover/focus—only the inner icon darkens to `82%` opacity and scales to `1.06`. The final `MacMix` wordmark remains anchored at its original `-0.1em` bottom offset, remains a single `--ink` color, uses an `850` weight and the user-approved looser `-0.03em` letter spacing, and is intentionally quieter than the first footer pass at `8.5%` base opacity. Desktop keeps `clamp(140px, 18.5vw, 300px)` and mobile keeps `clamp(92px, 25vw, 132px)`; only the `641px–1120px` range expands toward the smaller of the desktop maximum and the viewport-safe width via `min(300px, calc((100vw - 16px) * 0.2745))`. Compensate the footer height across that range by the exact added line-box height so enlarging the wordmark never changes its visual gap from the social row. Its additional fade is created only by an opacity mask, never a color gradient: the mask is down to `42%` strength near `39%` of the wordmark height and reaches fully transparent at `90%`, keeping the bottom quieter without erasing it too early. Preserve the small transparent inline padding around the wordmark so the final `x` is never clipped by the footer overflow boundary.

All Music track titles, times, progress treatment, and transport icons use white. The secondary metadata line uses the same white at `82%` opacity and reads `dylanf - Canon in D (Piano Version) - Single`, `三亩地 - 城南花已开 - Single`, or `坂本龍一 - THREE`. Match Apple Music's transport treatment: previous and next each contain two separate small play-triangle icon elements joined tip-to-tail; the pause glyph is optically thickened; no skip bar is present. The progress control has no visible thumb and distinguishes played (`88%` white) from remaining (`30%` white) only through opacity, while preserving click and drag seeking. Do not restore a round progress knob.
