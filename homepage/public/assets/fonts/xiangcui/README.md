# 香萃等粗宋

Converted from the locally installed `XiangcuiDengcusong.ttf`.

- `XiangcuiDengcusong.woff` retains the complete font.
- `hello-title.woff` contains `我是` and is the small subset loaded by the homepage.

When changing the Chinese introduction title, regenerate the subset and update
its CSS `unicode-range`. Latin characters continue to use Exposure.
